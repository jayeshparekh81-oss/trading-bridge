# orderflow_engine / recorder — pinned runtime deps (Python 3.12)
websockets==12.0
pyarrow==16.1.0
PyYAML==6.0.1
psycopg[binary]==3.2.1
cryptography==42.0.8
boto3==1.34.140
# uvloop is optional (speeds up the asyncio loop); safe to omit on non-Linux
uvloop==0.19.0

# --- test/dev only ---
pytest==8.2.2
