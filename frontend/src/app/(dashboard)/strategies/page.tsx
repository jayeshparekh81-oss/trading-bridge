import { ComingSoon } from "@/components/coming-soon";

export default function StrategiesPage() {
  return (
    <ComingSoon
      pageName="Strategies"
      description="Manage trading strategies, configure entry lots / partial-profit lots / AI validation per strategy, copy webhook URLs. Backend CRUD is ready — UI in next sprint."
    />
  );
}
