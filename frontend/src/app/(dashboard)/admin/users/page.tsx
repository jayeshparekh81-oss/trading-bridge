import { ComingSoon } from "@/components/coming-soon";

export default function AdminUsersPage() {
  return (
    <ComingSoon
      pageName="Users (admin)"
      description="User management for admin operators — activate / deactivate, role assignments, kill-switch reset. Backend admin endpoints exist — UI in a later sprint (admin-feature scope)."
    />
  );
}
