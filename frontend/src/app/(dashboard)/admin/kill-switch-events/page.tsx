import { ComingSoon } from "@/components/coming-soon";

export default function AdminKsEventsPage() {
  return (
    <ComingSoon
      pageName="KS events (admin)"
      description="Detailed kill-switch event timeline across all users. Today the user-scoped history lives on /kill-switch — this admin view is for ops scale and arrives in a later sprint."
    />
  );
}
