import { ComingSoon } from "@/components/coming-soon";

export default function WebhooksPage() {
  return (
    <ComingSoon
      pageName="Webhooks"
      description="View incoming webhook hits per token, recent triggers with IP audit, TradingView setup guide. Needs a new 'recent hits' aggregation endpoint — landing in next sprint."
    />
  );
}
