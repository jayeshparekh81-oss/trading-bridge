import { ComingSoon } from "@/components/coming-soon";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      pageName="Analytics"
      description="Win rate, equity curve, P&L distribution, per-strategy comparison, max drawdown, Sharpe. Needs aggregation endpoints + chart wiring — scheduled for the dedicated analytics sprint."
    />
  );
}
