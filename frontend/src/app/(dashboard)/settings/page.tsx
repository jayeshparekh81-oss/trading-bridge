import { ComingSoon } from "@/components/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      pageName="Settings"
      description="Profile, timezone, theme, change password, 2FA setup, API keys management. Backend endpoints exist for profile updates — wire-up in next sprint."
    />
  );
}
