import { ComingSoon } from "@/components/coming-soon";

export default function BrokersPage() {
  return (
    <ComingSoon
      pageName="Brokers"
      description="Connect Dhan, Fyers, Zerodha and other brokers. Manage credentials, test connections, refresh tokens. Backend endpoints are ready (full CRUD + reconnect) — wire-up in next sprint."
    />
  );
}
